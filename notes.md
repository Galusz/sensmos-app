## SENSMOS 1.5.56

- Fix: with a message collapsed into the bar, the node list jumped and scrolling snapped back — the message section was disposed off-screen and rebuilt with an empty state; it now stays alive and the collapsed state is ready from the first frame
